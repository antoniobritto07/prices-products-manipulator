import express from 'express';
const router = express.Router();

// import {
//     getAllPacks,
// } from '../controllers/PackController.js';

// router.get("/teste", getAllPacks);

export default router;